 #include<stdio.h>
 struct area
 {
 	float length;
 	flaot width;
 }a;
 main()
 {
 	int area;
 	printf("enter the length:");
 	scanf("%f",&a,length);
 	printf("enter the width:");
 	scanf("%f",&a,width);
 	area=a.width*a.length;
 	printf("%f",area); 	
}
